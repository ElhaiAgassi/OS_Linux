#ifndef CODECB_H
#define CODECB_H

// Function prototype for codecB encoding function
void codecB_encode(char* str);

// Function prototype for codecB decoding function
void codecB_decode(char* str);

#endif // CODECB_H
